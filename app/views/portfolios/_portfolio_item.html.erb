<div class="card" data-id="<%= item.id %>">
  <%= image_tag item.thumb_image, width: '100%' unless item.thumb_image.nil? %>
  <p class="card-text">
    <span><%= link_to item.title, portfolio_path(item) %></span>
    <%= item.subtitle %>
  </p>
</div>